# pubspec.yaml

name: devocional_nuevo
description: A new Flutter project.
publish_to: 'none'
version: 1.7.6+80

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  package_info_plus: ^8.0.2
  flutter_native_splash: ^2.3.1
  url_launcher: ^6.3.1
  http: ^1.1.0
  shared_preferences: ^2.5.3
  screenshot: ^3.0.0
  share_plus: ^11.0.0
  auto_size_text: ^3.0.0
  path_provider: ^2.1.5
  provider: ^6.0.0
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  intl: 0.20.2
  cached_network_image: ^3.3.0
  in_app_update: ^4.2.3
  in_app_review: ^2.0.8
  google_fonts: ^6.3.0
  lottie: ^3.1.0
  shimmer: ^3.0.0
  sqflite: ^2.3.0
  path: ^1.9.0
  scrollable_positioned_list: ^0.3.8
  card_swiper: ^3.0.1

  flutter_localizations:

    sdk: flutter
  # Push Notifications
  flutter_local_notifications: ^19.3.0
  timezone: ^0.10.1
  permission_handler: ^12.0.1
  flutter_timezone: ^4.1.1 #para obtener la zona horaria del dispositivo real

  # Firebase dependencies
  firebase_core: ^3.15.0
  firebase_messaging: ^15.1.3
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.3
  firebase_analytics: ^11.3.3
  firebase_crashlytics: ^4.1.3
  firebase_in_app_messaging: ^0.8.0+8
  firebase_remote_config: ^5.5.0

  # Audio dependencies
  flutter_tts: ^4.1.0
  synchronized: ^3.1.0+1  # For thread safety and mutex protection

  # Google Drive Backup dependencies
  googleapis: ^15.0.0
  google_sign_in: ^6.2.1
  extension_google_sign_in_as_googleapis_auth: ^2.0.12
  connectivity_plus: ^6.0.5
  archive: ^4.0.0

  # Modularización del proyecto
  bible_reader_core:
    path: bible_reader_core
  yaml: ^3.1.3

  clock: ^1.1.0



  flutter_cache_manager: ^3.4.1
  in_app_purchase: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  patrol: ^3.13.1
  flutter_lints: ^6.0.0
  mocktail: ^1.0.0
  bloc_test: ^10.0.0
  coverage: ^1.7.2
  mockito: ^5.4.4
  build_runner: ^2.4.12
  fake_async: ^1.3.0

  path_provider_platform_interface: ^2.1.2
  test: ^1.25.7


  firebase_analytics_platform_interface: ^4.4.3
  # Downgraded from 0.7.7 to 0.7.4 due to dependency conflict with integration_test from Flutter SDK
  # integration_test package pins test_api to 0.7.4, causing version resolution failure
  test_api: ^0.7.4
  firebase_remote_config_platform_interface: any
flutter:
  uses-material-design: true
  assets:
    - i18n/
    - assets/images/splash_background.png
    - assets/icons/
    - assets/badges/
    - assets/lottie/
    - assets/lottie/pets/
    - assets/biblia/
    - assets/fonts/DancingScript-Bold.ttf
    # Add Poppins font files here when available
  fonts:
    - family: DancingScript
      fonts:
        - asset: assets/fonts/DancingScript-Bold.ttf
          weight: 700
    # Uncomment and add Poppins font files when available
    # - family: Poppins
    #   fonts:
    #     - asset: assets/fonts/Poppins-Regular.ttf
    #       weight: 400
    #     - asset: assets/fonts/Poppins-Bold.ttf
    #       weight: 700
